spring:
  cloud: 
    nacos:
      discovery:
        namespace: bc0f01cb-1d82-4b37-b72b-198abbb4bc87
        server-addr: localhost:8848
        cluster-name: ${spring.application.name}
        service: ${spring.application.name}
        ip: ${spring.cloud.client.ip-address}
        port: ${server.port}

      config:
        namespace: bc0f01cb-1d82-4b37-b72b-198abbb4bc87
        server-addr: localhost:8848
        cluster-name: ${spring.application.name}